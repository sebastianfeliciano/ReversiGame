package model;

import controller.DirectionsEnum;
import controller.Player;
import controller.PlayerType;

import java.awt.*;

public class MaxMinStrategy implements Strategy {
    @Override
    public DirectionsEnum chooseMove(PlayerType player) {
        return null;
    }
}
